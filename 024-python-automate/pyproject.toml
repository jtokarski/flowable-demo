[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-automate"
version = "0.0.1"
description = "Local Development environment automation tools"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
