[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-tensorflow"
version = "0.0.1"
description = "Tensorflow Lab"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
