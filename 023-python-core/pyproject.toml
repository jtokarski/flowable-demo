[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-core"
version = "0.0.1"
description = "Demos and Cheat-Sheets for core Python APIs"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
